# firefinds package

Install from this directory:

```bash
pip install -e ".[dev]"
firefinds ingest-stub|ingest-live|score|rank
```

See the repository root README for scoring rules, gates, and secrets pattern
(`RANDMAR_CLIENT_ID` + `RANDMAR_CLIENT_SECRET_FILE`).
